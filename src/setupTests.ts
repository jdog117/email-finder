// jest-dom workaround isnt workarounding https://markus.oberlehner.net/blog/using-testing-library-jest-dom-with-vitest/
// continuing without it
// if i manually import jest-dom, things work well atm: import "@testing-library/jest-dom";

// import matchers from "@testing-library/jest-dom/matchers";
// import { expect } from "vitest";

// expect.extend(matchers); //extending with jsdom matchers
